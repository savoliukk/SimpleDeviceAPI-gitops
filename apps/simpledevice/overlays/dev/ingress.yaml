apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simpledevice-api
  namespace: simpledevice-dev
spec:
  ingressClassName: nginx
  rules:
  - host: dev.simpledevice-api.savoliukk.pp.ua
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: simpledevice-api
            port:
              number: 80
